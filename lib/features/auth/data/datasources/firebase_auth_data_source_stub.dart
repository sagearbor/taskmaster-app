// Stub implementation until Firebase is configured
import 'dart:async';
import 'auth_remote_data_source.dart';
import 'mock_auth_data_source.dart';

// For now, just extend MockAuthDataSource
class FirebaseAuthDataSource extends MockAuthDataSource {
  // Will implement with real Firebase when configured
}