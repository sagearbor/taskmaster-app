// Stub implementation until Firebase is configured
import 'task_remote_data_source.dart';
import 'mock_task_data_source.dart';

// For now, just extend MockTaskDataSource
class FirebaseTaskDataSource extends MockTaskDataSource {
  // Will implement with real Firebase when configured
}